import { NgComponentOutletDirective } from './ng-component-outlet.directive';

describe('NgComponentOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new NgComponentOutletDirective();
    expect(directive).toBeTruthy();
  });
});
